# MATH30000-Double-Project

Using uv for dependency control.

First make sure uv is installed.

Then run uv sync to sync environment.

Then to activate the virtual environment, run:
source .venv/bin/activate (linux)
.venv\Scripts\activate (windows).

Run deactivate to deactivate the environment

## TODO and ideas
put the point map creation into experiment and save as a file to reduce analysis wait time (could make a displayer class and keep analyser as a class that only calculates the analysis but does not display.)

work out how to make point map images into video.